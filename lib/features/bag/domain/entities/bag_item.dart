class BagItem {
  const BagItem(
      {this.title, this.imgPath, this.code, this.price, this.quantity});
  final String title, imgPath, code, price, quantity;
  // final Product product instead of individual props
}
